node_modules/
dist/
docs/
site/
.DS_Store
*.log

# Docusaurus site (website/) generated output
website/build/
website/.docusaurus/

# npm's package-lock.json is the committed, canonical lockfile.
# Ignore the alternative package managers' lockfiles to keep a single source.
pnpm-lock.yaml
bun.lock
bun.lockb

# Session state / credentials (NEVER commit to git!)
.whatsweb_auth/
.wwebjs_auth/
auth_info*/
